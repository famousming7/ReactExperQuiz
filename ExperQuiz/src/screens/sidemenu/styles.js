import {Colors} from '@theme';

export default {

    container: {
        flex: 1, 
        paddingTop:40,
        flexDirection: 'column',
        width:'100%',
        backgroundColor:Colors.lightGrayColor
    }
}