import {Colors} from '@theme';

export default {

    container: {
        flex: 1, 
        marginTop:0,
        flexDirection: 'column',
        width:'100%',
        backgroundColor:Colors.whiteColor
    }
}